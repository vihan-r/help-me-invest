/** Trailing arrow glyph for CTAs. Nudges forward on hover inside buttons (see CSS). */
export function Arrow() {
  return (
    <span aria-hidden="true" className="anim-arrow">
      →
    </span>
  );
}
