import { PagePlaceholder } from "@/components";

export default function Home() {
  return (
    <PagePlaceholder eyebrow="Help Me Invest" title="Property investing, on your own terms">
      The home page is built in a later stage. The global shell, navigation, and design system are
      in place — use the nav to move around the site.
    </PagePlaceholder>
  );
}
